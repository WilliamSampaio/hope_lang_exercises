uses core;

dec qsoma : num # num -> num;
dec cubo : num -> num;
dec prod : num # num -> num;
dec imc : num # num -> num;
dec brl_usd : num # num -> num;
dec squad : num # num -> num;

--- qsoma (x, y) <= pow (x + y, 2);

--- cubo x <= pow (x, 3);

--- prod (x, y) <= x * y;

--- imc (peso, altura) <= peso / pow (altura, 2);

--- brl_usd (vbrl, usd) <= vbrl / usd;

--- squad (x, y) <= pow (x, 2) + pow (y, 2);
